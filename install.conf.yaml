- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/polybar: polybar
    # ~/.config/i3: i3
    ~/.zshrc: zshrc     
    ~/.vimrc: vimrc      
    ~/.config/termite: termite
    ~/.config/beets/config.yaml: beets/config.yaml
    ~/.config/mpd/mpd.conf: mpd/config
    ~/.ncmpcpp/config: ncmpcpp/config
    # ~/.config/compton.conf: compton/compton.conf
    # ~/.config/keepmenu: keepmenu 
    # ~/.i3scripts: scripts
    # ~/.config/rofi/config.rasi: rofi/rofi.conf
    # ~/.config/rofi/my_theme.rasi: rofi/my_theme.rasi
- shell:
  - [git submodule update --init --recursive, Installing submodules]
